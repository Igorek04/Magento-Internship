<template x-if="catalogData.totalCount === 0 && !catalogData.isLoading">
    <div class="message info empty" style="margin: 20px 0;">
        <span><?= __('We can\'t find products matching the selection.') ?></span>
    </div>
</template>
