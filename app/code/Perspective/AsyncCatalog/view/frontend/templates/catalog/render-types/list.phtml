<?php
use Magento\Framework\View\Element\Template;
/** @var Template $block */
$layout = $block->getLayout();
?>

<?= $layout->renderElement('toolbar.top') ?>

<div class="products wrapper list products-list">
    <ol class="products list items product-items">
        <template x-for="product in catalogData.products" :key="product.id">
            <li class="item product product-item">
                <div class="product-item-info" data-container="product-list">
                    <?= $layout->renderElement('item.elements.image') ?>

                    <?= $layout->renderElement('item.elements.details') ?>
                </div>
            </li>
        </template>
    </ol>
    <?= $layout->renderElement('catalog.root.listing.additional') ?>
</div>

<?=  $layout->renderElement('toolbar.bottom') ?>
